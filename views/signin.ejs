<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Grid Password Login</title>
    <link rel="stylesheet" href="/signin.css">
</head>
<body>
    <div id="login-form">
        <h2>Login</h2>
        <form onsubmit="return handleLogin(event)">
            <input type="email" name="email" id="email" placeholder="Enter your email" required>
            <input type="number" name="gridSize" id="gridSize" placeholder="Enter grid size (2-10)" min="2" max="10" required>
            <select name="gridShape" id="gridShape" required>
                <option value="square">Square Grid</option>
                <option value="circle">Circular Grid</option>
                <option value="triangle">Triangle Grid</option>
            </select>
            <input type="hidden" name="pattern" id="pattern">
            <input type="password" id="password" placeholder="Click to enter password" onclick="showGridPopup()" readonly required>
            <button type="submit">Login</button>
        </form>
        <p id="loginMessage"></p>
    </div>

    <div class="popup-container" id="popup">
        <h3>Enter Your Password Pattern</h3>
        <div class="grid" id="gridContainer"></div>
        <button onclick="verifyPassword()">Submit</button>
    </div>

    <script src="/signin.js"></script>
</body>
</html>
